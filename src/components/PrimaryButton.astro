---
interface Props { href: string; class?: string; }
const { href, class: className } = Astro.props;
---
<a class:list={["button", "button--primary", className]} href={href}><slot /><span aria-hidden="true">→</span></a>
