---
interface Props { href: string; class?: string; }
const { href, class: className } = Astro.props;
---
<a class:list={["button", "button--secondary", className]} href={href}><slot /><span aria-hidden="true">→</span></a>
